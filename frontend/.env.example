# Local development — copy to frontend/.env.local
NEXT_PUBLIC_API_URL=http://localhost:4013/api

# Production on EC2 — use frontend/.env.production (see .env.production.example):
# NEXT_PUBLIC_API_URL=https://api-ranhill.atenxion.ai/api
