# Copy to frontend/.env.local for local dev
NEXT_PUBLIC_API_URL=http://localhost:4013/api

# Production (PM2): copy .env.production.example → frontend/.env.production
# Edit NEXT_PUBLIC_API_URL to your public API URL, then rebuild + pm2 reload
