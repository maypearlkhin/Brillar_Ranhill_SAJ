# Production — put this file on the server as frontend/.env.production
# Then rebuild: cd frontend && npm run build && pm2 restart ranhill-saj-frontend
NEXT_PUBLIC_API_URL=https://api-ranhill.atenxion.ai/api
